#include<iostream>
using namespace std;
#include<string.h>
int main()
{
    char a[20];
    cout<<"Enter a string:";
    cin>>a;
    cout<<strrev(a);
    // if(a==strrev(a))
    // cout<<"P";
    // else
    // cout<<"nP";
    return 0;
}